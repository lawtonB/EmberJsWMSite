import { linkHider } from 'wm-site/helpers/link-hider';
import { module, test } from 'qunit';

module('Unit | Helper | link hider');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = linkHider([42]);
  assert.ok(result);
});
